# Prometheus Project: Infinity — backend dependencies.
# Install:  pip install -r requirements.txt
# Run:      uvicorn app.main:app --host 0.0.0.0 --port 8443
fastapi>=0.104,<0.116
uvicorn[standard]>=0.24,<0.31
psycopg2-binary>=2.9,<3
PyJWT>=2.8,<3
websockets>=12,<14
